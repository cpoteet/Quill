# Quill

Native macOS app for writing and managing WordPress content. Built with Swift Package Manager (no Xcode needed).

## Status

Implementation complete and running. Active polish/iteration phase.

## Build & run

```bash
./build.sh        # compiles, assembles Quill.app, ad-hoc signs
open Quill.app
./test.sh                                # run ALL tests (Swift + JS editor) — use this
swift test                               # Swift tests only
swift test --filter WordPressClientTests # one Swift suite
node --test Scripts/test-block-serializer.js   # block parse/serialize round-trip corpus, cursor walk, wrap decisions only
node --test Scripts/test-editor-preservation.js # unsupported-block preservation + data-loss alarm only
node --test Scripts/test-editor-containers.js  # container block (columns/details/buttons/accordion/tabs) tests only
node --test Scripts/test-editor.js          # JS transform tests only
node --test Scripts/test-editor-keyboard.js # live editor keyboard tests only
node --test Scripts/test-editor-gallery.js  # gallery node insert/parse/render tests only
node --test Scripts/test-editor-passthrough.js # unmodeled Gutenberg block passthrough tests only
node --test Scripts/test-editor-paste.js    # clipboard paste + insertMarkdown tests only
node --test Scripts/test-editor-footnotes.js # core/footnotes meta split, load/save/migration only
node --test Scripts/test-block-settings-registry.js # block-settings.js shape guard only (pure Node)
node --test Scripts/test-editor-block-settings.js   # per-setting round-trips, generated controls, the fixture corpus
```

**After every code change:** quit the app, run `./build.sh`, reopen. Always — `build.sh` replaces the binary under a running process, so skipping the quit leaves the old app running and no visible change. If the build fails, report the error instead of opening.

```bash
osascript -e 'quit app "Quill"' 2>&1; sleep 2 && ./build.sh 2>&1 && open Quill.app
```

**Computer-use testing goes on a new local draft.** Click "+ New Post" (or File → New Post) first, and discard it when done. Never test edits on a published post or page — one Cmd+Z too many blows past the test edits and undoes the initial content load, emptying the editor.
**After major changes:** run the `claude-md-management:revise-claude-md` skill to keep this file current.

Requirements: Swift 6.3.1 (already installed), macOS 13+. JS tests require `node` (already installed) and `jsdom` — installed in **`Scripts/`** (`Scripts/package.json` + `Scripts/node_modules/`, gitignored), *not* the project root, which has no `package.json` at all. Consequence: an ad-hoc jsdom probe script must also live in `Scripts/`; one written to the scratchpad or the repo root dies with `Cannot find module 'jsdom'`.

## Test suite status (2026-09-14 — 388 Swift + 957 JS tests, all passing)

Counts verified by running each suite on 2026-09-14. If you touch a suite, re-run it and correct the number rather than trusting the prose below.

**Swift (388 tests):** 26 suites covering all models (including wpautop classic-content handling — now also gated on a `wp-block-` class substring so class-only Gutenberg passthrough content isn't misclassified as classic — HTML entity decoding, `WPMedia.sizedURL(for:)` size-slug resolution with blank-URL and "full" fallback, and `WPMedia.caption`/`captionText` plain-text decoding across raw-present, rendered-only, and absent payloads), WordPressClient (incl. `+`-in-query escaping), all storage layers, AIPromptBuilder (including list/table context-aware prompts with correct `<ul>`/`<ol>` tag selection, phantom punctuation-spacing suppression, `<cite>` wrapper stripping), AnthropicClient (incl. optional `stop_reason` decoding and `AnthropicError.networkError` friendly messaging), AppState view-model logic, dropped-image upload feedback strings (`uploadStatusText`/`uploadSuccessMessage`/`uploadFailureMessage` — singular vs. plural, the "Converted to JPEG" note, single-file error passthrough vs. batch failure counts), EditorCoordinator (incl. `mediaSizesDict(for:)`'s "full"-entry fallback), status helpers, PostStats, MimeType, `ImageConversion` (17 tests: HEIC/HEIF→JPEG upload conversion incl. uppercase `.HEIC`, EXIF-orientation *and* pixel-dimension survival, filename handling — extension-only replacement, extensionless pass-through, two same-named files landing in separate per-upload temp dirs — whole-temp-directory cleanup, pass-through for JPEG/PNG/PDF, and fallback to the original when ImageIO cannot decode the file), UpdateChecker version comparison, `BlockRiskAlarm` (11 tests: the three banner stages' title/body copy, singular vs. plural wording, block display names incl. the four that need a friendly label, an em-dash guard across every string, and that only the unacknowledged stage blocks saving), and a regression guard confirming `wp:gallery`/`wp:image` block-comment content survives `editorHTML` unchanged. Each network suite uses its own MockURLProtocol subclass to avoid global-state races.

**JS block serializer (79 tests):** `Scripts/test-block-serializer.js` covers the `window.BlockParser` bundle, the tree → `post_content` serializer, the real-`post_content` byte-identity corpus in `Scripts/fixtures/`, and the block descriptor registry. Pure Node — no DOM, no Tiptap — so it is the cheapest guard against the round-trip regression class that produced the three shipped comment-stripping bugs. Since 2026-09-12 it also covers the unsupported-block machinery: `blockSourceSlices`'s cursor walk (exact slices for canonical output and for every real fixture, inter-block whitespace preserved as freeform slices, the serialised fallback for the five known non-canonical attribute forms, and guards that an inexact block — nested or self-closing — still yields a faithful source for the blocks after it), `blockNeedsWrapping` and `wrapUnsupportedBlocks` (every block Quill models is left alone, the four real-site fixtures come back untouched, and exactly the eight unsupported blocks in the corpus fixture are wrapped), and `unrepresentedBlockNames`.

**JS preservation (32 tests):** `Scripts/test-editor-preservation.js` uses the real-`editor.html`-in-jsdom approach to cover unsupported-block preservation end to end, because the wrap/unwrap path crosses `setContent`, the passthrough node's parse rule and `getContent`, none of which the pure helpers reach. Covers the parser and serializer being live in the editor, the wrapper parsing into a `gutenbergPassthrough` card with a truncated monospace peek rendered as text rather than DOM, the wrap applying at exactly the two `post_content` entry points (and neither AI reject path), code view still showing real source, the `Scripts/fixtures/unsupported-blocks.html` corpus surviving load → edit → save byte-for-byte and idempotently, and the data-loss alarm — silent on every fixture, and firing with the right block names when the wrap is stubbed out, which is the only way to prove the tripwire is independent of the fix it guards.

**JS containers (264 tests):** `Scripts/test-editor-containers.js` uses the real-`editor.html`-in-jsdom approach to cover the container block nodes (columns, details, buttons, accordion, tabs) end to end: insert, type-into, parse from real WordPress markup, the contextual toolbar controls, the insert menu, pullquote and preformatted, and Phase 5/6's block-attribute carrying and ownership — including the guard that a column's `width`, which Quill has no UI for, survives an edit untouched. Also covers the empty-title hint on accordion headings and details summaries — the `is-untitled` node decoration, the CSS that draws the hint, and the deferred caret placement an untitled row needs. Since 2026-09-13 it also covers the two block-level gestures every container shares — the ✕ / ⌘⇧⌫ delete and the Esc / double-Enter exit — across all ten deletable types, innermost-first nesting, the created-vs-reused landing paragraph, the menu deferral on Esc, the behaviours deliberately left alone (table Enter and Tab, mid-panel splits), and the citation control in both quote blocks. Since 2026-09-14 it also covers the table figure (its classes and a Gutenberg-authored caption), head/body/foot row sections and their toggles, the table style toggle, the Word-style table size picker, and the separator entry in the insert menu.

**JS transforms (211 tests):** `Scripts/test-editor.js` covers `toWordPressHTML`, `extractAlignment`, `formatHTML`, `countStats`, `findMatches`, `findMatchesLoose`, `fuzzyAnchorRegex`, `detectEmbedProvider`, `embedClassFor`, and the `gutenbergPassthrough` parsing helpers (`passthroughLabelFromClass`, `passthroughLabelFromBlockName`, `parsePassthroughBlock`, `isModeledFigure`/`QUILL_MODELED_FIGURE_CLASSES` — incl. a drift guard that the modeled set is exactly image/gallery/embed/table, and that matching is exact `classList` membership, not the substring the parse selector uses, so `wp-block-image-slider` is not handed to the image rule) via Node + jsdom (headings, lists, blockquotes, code blocks, horizontal rules, images, tables incl. Tiptap artifact cleanup, embeds, footnotes, footnote backrefs, gallery block-comment wrapping incl. idempotency, gallery image captions surviving the save transform (the generic figure pass reaches nested gallery figures, so there is no gallery-specific caption pass) with DOM-structural guards that each caption stays the last child of its *own* nested image figure — not hoisted onto the wrapper or attached to the wrong image — and lands inside its own `wp:image` comment pair, unicode, whitespace-tolerant anchor matching, CR-before-`-->` comment stripping, unmodeled-Gutenberg-block passthrough incl. nested-content byte-for-byte preservation, standalone-image `wp:image` comment wrapping incl. attribute derivation, gallery non-interference, idempotency and `data-media-id` removal). Since 2026-09-13 it also covers image dimensions and the decorative flag (15 tests): `width`/`height` attributes converted to core's inline `style` with a matching `is-resized` class and `width`/`height` comment attrs, `height:auto` when only a width is set, a stale `is-resized` stripped from an unresized figure, and `role="none"`/`"presentation"` producing the `isDecorative` comment attr Gutenberg's `save()` needs to regenerate the role.

**JS editor keyboard (70 tests):** `Scripts/test-editor-keyboard.js` loads the **real `editor.html`** in jsdom, instantiates the live Tiptap editor via the `window._tiptapEditor` global, dispatches real `keydown` events, and asserts on the resulting ProseMirror document. This is the only automated coverage of the live Enter/Backspace/Shift-Enter handlers (the code paths that caused the June 2026 regression chain). Covers plain paragraphs, headings, lists, blockquotes/cite, footnotes (soft-break Enter, Backspace), image captions, class-preservation through schema round-trips, and image link-to-full-size (`linkTo`/`linkHref` attrs, incl. classic non-figure markup and empty-href handling) — each with the regression commit referenced inline where applicable. Also covers WP 7.1's "mark as decorative" image toggle: `role` on the `<img>` parses into the `imgRole` attr and survives `toWordPressHTML`, under both the `figure.wp-block-image` rule and the bare `img[src]` fallback, with no `role` emitted when the source had none, `role` landing on the `<img>` rather than the `<a>` when the image also links to full size, and `role=""` treated as absent. Since 2026-08-20 it also covers `window.insertImage`'s **cursor placement** (14 tests): the cursor lands in a new empty paragraph *after* the figure rather than inside its empty `<figcaption>`, the caption stays empty but keeps the image attrs, consecutive inserts reuse the trailing paragraph instead of stacking blanks, a multi-image drop saves one `wp:image` pair per image in drop order (idempotent through the save transform), and the insert stays scoped to a list item / blockquote / table cell, lands after a code block, and is refused inside a footnote. A separate `image dimensions round-trip` block (10 tests) loads core's real resized-image markup and asserts the style, `is-resized` class and comment attrs survive a save, that legacy `width`/`height` attributes are converted, and that a Quill-inserted sized image saves core-compatible markup. **jsdom caveat:** ProseMirror only keymap-binds Backspace at node boundaries (joinBackward/lift); mid-text character delete is browser `beforeinput`, which jsdom does not emit, so only boundary Backspace is asserted. Polyfills `crypto.randomUUID`/`matchMedia`/`requestAnimationFrame`/`ResizeObserver`; the `document.execCommand` jsdom error from `onCreate` is harmless.

**JS gallery node (36 tests):** `Scripts/test-editor-gallery.js` uses the same real-`editor.html`-in-jsdom approach as the keyboard tests to exercise the `galleryBlock` node's insert/parseHTML/renderHTML/`sourceHTML`-round-trip behavior end-to-end — a custom Tiptap node's parse/render logic can't be exercised through the pure `editor-transforms.js` helpers alone. Includes `sizeSlug` coverage: extraction from a loaded figure's `size-*` class on parse, propagation through the `window.insertGallery` JSON bridge, and honoring a non-default value in the reconstruction render path. Includes `fullUrl` coverage: `linkTo: 'media'` links to the image's `fullUrl` (true full-resolution original) rather than its display-size `url`, with a fallback to `url` when `fullUrl` is absent. Includes per-image `alt` and `caption` coverage — both come straight from `GallerySheet` `TextField`s, so both are asserted to serialize as data, never markup: the caption renders as an escaped `figcaption.wp-element-caption` inside its own image figure (via `textContent`, never `innerHTML`), omitted entirely when empty, and is extracted back into node attrs on parse; alts land on their own `<img>` in payload order, still emit `alt=""` when omitted, and can't break out of the quoted attribute value. Two round-trip suites close the loop: `insert → save → re-parse` runs the exact `PostEditorView`/`GallerySelection` JSON payload through `window.insertGallery` and back (per-image ordering, own-`wp:image`-comment containment, idempotency), and `captions survive load → edit → save` re-exercises the greedy-comment-strip regression class with captions present — an intervening visual edit must not consume an image figure or duplicate a caption.

**JS passthrough node (35 tests):** `Scripts/test-editor-passthrough.js` uses the same real-`editor.html`-in-jsdom approach to exercise the `gutenbergPassthrough` node's parse/render behavior end-to-end — catches any `wp-block-*` classed, non-`<figure>` element no other parse rule claims (Accordion, Columns, Group, etc.), preserves its `outerHTML` verbatim, and regenerates any original `<!-- wp:name -->` comments on save. Covers class-only markup, comment-wrapped markup, nested real blocks (e.g. a `wp:image` nested inside a passthrough `wp:group`) surviving save, and specificity guards confirming the catch-all never steals elements `galleryBlock`/`image`/`embedBlock`/`table`/`heading` already claim. Since 2026-08-19 it also claims **figure-rooted** unmodeled blocks — WP 7.1's `wp:playlist` plus the long-broken `wp-block-audio`/`wp-block-video`/`wp-block-pullquote` figures — with matching guards that the four figures Quill does model (`QUILL_MODELED_FIGURE_CLASSES`) still reach their own nodes, that a passthrough figure keeps its position in the top-level node sequence, that repeated load/save cycles don't stack block comments, and that a classic `<figure>` with no `wp-block-*` class still parses as an image.

**JS paste (19 tests):** `Scripts/test-editor-paste.js` uses the same real-`editor.html`-in-jsdom approach to cover both clipboard branches (see the paste-path gotcha in `Sources/QuillKit/Resources/CLAUDE.md`) plus `window.insertMarkdown`. Covers footnote containment for plain-text *and* HTML pastes, body/code-block paste left unchanged, and Markdown conversion of headings/lists/quotes/fences/rules/tables/images, task-list degradation, `<script>` stripping, and the footnote/code-block/code-view refusals. **jsdom caveat:** jsdom has no `DataTransfer`, so the suite hand-builds `event.clipboardData` — it exercises the flavors the test constructs, not what WKWebView actually delivers (RTF, webarchive, Word conditional-comment HTML, syntax-highlighted HTML from code editors). Treat green here as weaker evidence than the keyboard suite and confirm real pastes in the app.

**JS footnotes (36 tests):** `Scripts/test-editor-footnotes.js` uses the real-`editor.html`-in-jsdom approach to cover `core/footnotes`, whose bodies live in post meta rather than `post_content` (see `docs/footnotes-meta.md`). Covers loading a delimiter-plus-meta post into an editable list, a delimiter with no meta falling through to a passthrough card, saving content that carries the delimiter and never the list, `getFootnotes()` matching what core stores, an edited body reaching the meta, core's `<fnId>-link` marker anchor, no backref in `post_content`, byte-identical save of an unedited post, idempotency across a second load/save cycle, deleting the last marker clearing the meta, the legacy-inline-list migration (untouched on load, moved to meta on first edit, legacy backref stripped), two-footnote ordering and renumbering, inserting a brand-new footnote into a post that had none, and code view showing the delimiter rather than the list (the one place `getContent`'s extraction had to be duplicated, because `_enterCodeView` builds its own source string). Also covers the two backref-chrome CSS rules — an empty item's backref is hidden via an `fn-item-empty` class the node view sets (the trailing break must stay in layout — hiding it makes the item uneditable in WebKit, and a `:has()` on the break is not re-evaluated when the break goes, so the backref never returns), and the backref and footnote marker opt out of the ⌘-held underline affordance (neither is ⌘-clickable) with a guard that the opt-out still follows and out-specifies the rule it overrides.

**JS block settings (13 + 147 tests):** `Scripts/test-block-settings-registry.js` is pure Node and guards the registry's own shape — every entry's `kind`, the fields each kind must declare, that no entry names a block with no descriptor, and (by reading its own source) that no block key is repeated, which silently discards the earlier one with no runtime error. `Scripts/test-editor-block-settings.js` loads the real `editor.html` in jsdom and covers each setting end to end: the generated Tiptap attribute, the generated delimiter key, and the generated toolbar control. Since 2026-09-14 it also covers the accordion icon controls, which core stores twice — on `core/accordion` and again on every `core/accordion-heading` — so the control writes the block and every heading in one transaction, one undo reverses both, and an item added afterwards inherits them. Two suite-wide guards close it: every `settings-*.html` fixture comes back untouched with no edit and saves idempotently once edited, and a drift guard fails if a registry entry's name appears in no test.

**Full reference:** `docs/testing-plan.md` — lists every test by name with what it checks, plus the manual/functional checklists for release sign-off.

## Test suite gotchas

- **Each network test suite needs its own `URLProtocol` subclass** — `@Suite(.serialized)` only serializes within a suite; two serialized suites sharing `MockURLProtocol.requestHandler` (a global static) race against each other. Solution: give each suite its own subclass with its own `static var requestHandler` (e.g. `AnthropicMockURLProtocol` in `Tests/QuillTests/Support/`).
- **`httpBody` is always nil in `URLProtocol.startLoading()`** — URLSession moves the body to `httpBodyStream`. To inspect request bodies in mock tests, reconstruct from the stream. See `AnthropicMockURLProtocol.startLoading()` for the pattern.
- **DOM-wrap + string-strip round-trips can leave orphaned whitespace text nodes** — when a transform inserts a separator (e.g. `\n\n`) *between* two wrapped elements rather than adjacent to either one's own delimiter, a later strip-by-regex pass can't fully remove it (each strip regex only consumes whitespace touching its own comment tag), so the separator survives as a stray whitespace-only text node between the bare elements. Re-wrapping then stacks a new separator on top instead of replacing it, and the output grows on every save. Fix at the DOM level, scoped to the exact container being re-wrapped: strip whitespace-only child text nodes immediately before re-inserting separators, rather than adding more regex. Discovered in the gallery block-comment wrapper (`toWordPressHTML`'s `figure.wp-block-gallery` handling); relevant to any future block that wraps a repeated list of sibling elements with per-item comments.
- **Greedy `[^\n]*` in comment-stripping regexes can span multiple comments and delete the content between them** — `toWordPressHTML`'s upfront strip of pre-existing `wp:embed`/`wp:gallery`/`wp:image` comments used `<!-- wp:X [^\n]*-->` to match one comment's attrs. This assumes each comment is followed by a `\n` before the next one starts, which holds for freshly-wrapped content (the wrap step itself always inserts a `\n`) but not for a *loaded* gallery's `sourceHTML` (`galleryBlock`'s verbatim-re-render attr, captured from the original WordPress figure) — DOM whitespace-node collapsing during Tiptap's parse can leave zero characters between one image figure's closing `<!-- /wp:image -->` and the next image's opening `<!-- wp:image -->`. With no `\n` boundary, `[^\n]*` greedily matched past the *first* comment's own `-->` all the way to the *last* `-->` in the string, deleting every image figure in between — this is what caused a gallery's images to silently disappear after any visual edit (the `toWordPressHTML(editor.getHTML())` call on every debounced save re-triggers the strip). Fixed by making the attrs group non-greedy (`[\s\S]*?-->`), which always stops at the nearest `-->` regardless of surrounding whitespace. Use `[\s\S]*?`, not `.*?` — JS `.` excludes all line-terminator characters (`\n`, `\r`, U+2028, U+2029), not just `\n`, so a `.*?` group fails to match at all (leaving the comment unstripped) if a stray `\r` ever lands inside a comment's attrs before its own `-->`. Regression tests: `Scripts/test-editor.js`'s `'stripping pre-existing wp:image comments does not consume the images between them'` and `'stripping a pre-existing wp:gallery comment works even with a CR before its closing -->'`.
- **Edit tool corrupts quotes in JS test files** — when the Edit tool's `old_string` spans a region containing curly Unicode quotes (U+2018/U+2019), it can replace straight ASCII `'` delimiters with curly ones in the output, producing `SyntaxError: Invalid or unexpected token` in Node.js. If you see that error after editing `Scripts/test-editor.js`, the fix is a targeted Python byte-level replacement — do NOT use the Edit tool again to fix it, as it will re-introduce the same corruption. The existing unicode test on line 277 intentionally contains U+2019 as *content* (not delimiters) and must be left alone.
- **`test-editor-keyboard.js`: the first `setContent(figureHTML)` right after an `extendMarkRange('link')`-over-an-existing-mark call silently no-ops** — confirmed via `git stash` to reproduce on unmodified `editor.html`, so it's a pre-existing jsdom/Tiptap interaction, not caused by any one feature's code: `editor.chain().extendMarkRange('link').setLink(...).run()` over an *existing* link mark poisons exactly the next `setContent(...)` call, which produces an empty `<p></p>` instead of parsing the given HTML (only that one call — the next one parses fine). Reproduces with any figure/image HTML, not just linked images; never root-caused past "ProseMirror's stored marks/selection mapping through the full-doc `replaceWith` is the suspect." If a new `describe()` block's first test calls `setContent` with block-level HTML and lands right after a test exercising `extendMarkRange`, add a throwaway `editor.commands.setContent('<p></p>', false)` in a `before()` hook to absorb the one-shot quirk (see the `'image link-to-full-size'` describe block for the pattern).

## Key decisions

- **Stack:** Swift 6, SwiftUI (macOS 13+), WKWebView, URLSession async/await
- **Editor:** Tiptap 2.x inside WKWebView, loaded from a local bundle (`tiptap-bundle.js` in Resources). Bundled via `./Scripts/bundle-tiptap.sh` (requires `node`). To update Tiptap at any time, just tell Claude "check for new versions of Tiptap" — Claude will check the latest release, update the version in the script if needed, regenerate the bundle, update `editor.html` and `CLAUDE.md`, and rebuild.
- **API:** WordPress REST API with Application Passwords (no plugin required)
- **Storage:** SQLite.swift for local drafts/autosaves; credentials stored as JSON in `~/Library/Application Support/Quill/credentials.json` (chmod 600, not the system keychain — avoids password prompts)
- **URLSession:** Always use `URLSessionConfiguration.ephemeral` (default in `WordPressClient`) — prevents URLSession from touching the system keychain credential store

## Future architecture options

See `docs/future-architecture.md` for deferred design notes: local draft settings persistence (B), image figure-first model (C), LanguageTool grammar checking (D), generic Gutenberg passthrough (E — since implemented as the `gutenbergPassthrough` node; doc entry is the original design context), editor image cache-busting (F), native Pullquote block (G), a Gutenberg fixture-diff harness for automated markup-change detection (H), and a gradient-matched title bar (I).

## Architecture

```
Sources/QuillKit/
  App/              AppState, AppServices, QuillApp, AppSupportDirectory, UpdateChecker
  Auth/             CredentialsStore (file-based, not system keychain)
  API/              WordPressClient, Models (WPPost, WPMedia, WPTaxonomy), MimeType, ImageConversion
  AI/               AnthropicClient, AISettings, AISettingsStore, AIPromptBuilder
  Storage/          Database, DraftStore, AutosaveStore, TaxonomyCache
  Views/
    ContentView.swift
    Editor/         PostEditorView, EditorView, EditorCoordinator, DroppableWebView
                    TitleTextField, LinkPickerView, BlockRiskAlarm
    Sidebar/        SidebarView, PostListRow
    Settings/       PreferencesView, PostSettingsPanel, AboutView
    Media/          MediaPickerView, MediaDetailView, MediaSidebarSection, GallerySheet
    AI/             GeneratePostSheet, AIResultPanel, SamplePostPickerSheet
  Resources/        editor.html (Tiptap)
                    editor-transforms.js (WordPress HTML transforms, shared with test suite)
                    block-descriptors.js (Tiptap node → Gutenberg block map, used by the save transform)
                    block-settings.js (one entry per block setting; drives the Tiptap attribute, the delimiter key and the toolbar control)
                    block-parser-bundle.js + block-serializer.js (WordPress's own block parser and its inverse; loaded by editor.html for unsupported-block preservation)
  DesignSystem.swift
```

## Key files to know

- `Sources/QuillKit/Resources/editor.html` — entire Tiptap editor; JS↔Swift bridge via `window.webkit.messageHandlers.*` and `window.*` globals
- `Sources/QuillKit/Views/Editor/EditorCoordinator.swift` — WKWebView delegate + message handler; handles insert-image notification
- `Sources/QuillKit/Views/Editor/DroppableWebView.swift` — WKWebView subclass intercepting Finder image drops
- `Sources/QuillKit/API/WordPressClient.swift` — all REST API calls
- `Sources/QuillKit/API/ImageConversion.swift` — converts HEIC/HEIF to JPEG before upload (WordPress 7.1 accepts HEIC but can't generate sub-sizes for it). Used by all three upload paths: `PostEditorView.handleDroppedImages`, `MediaSidebarSection` upload-from-disk, and `uploadPickedImage` (`MediaPickerView` + `GallerySheet`)
- `Sources/QuillKit/Resources/editor-transforms.js` — `toWordPressHTML`, `extractAlignment`, `formatHTML`, `countStats`, `findMatches`, `findMatchesLoose`, `fuzzyAnchorRegex`, `detectEmbedProvider`, `embedClassFor`, `passthroughLabelFromClass`, `passthroughLabelFromBlockName`, `parsePassthroughBlock`; shared between `editor.html` and `Scripts/test-editor.js`. Loaded as `<script src="./editor-transforms.js">` before the main editor script block, so all functions are available as globals inside `editor.html`'s JS.
- **Paste as Markdown (⌘⇧V)** — `AppState.triggerPasteMarkdown` → `QuillApp`'s `CommandGroup(after: .pasteboard)` → `PostEditorView.pasteAsMarkdown()` reads `NSPasteboard` and calls `window.insertMarkdown(text)` (`editor.html`), which converts via `MarkedBundle.marked`, sanitizes, and inserts. Deliberately bypasses ProseMirror's clipboard plumbing entirely so ⌘V is unaffected; returns a status string (`ok`/`footnote`/`code-block`/`code-view`/`parse-error`/`empty`) that `pasteAsMarkdown` turns into a toast.
- **Footnotes live in post meta, not `post_content`** — `core/footnotes` is a dynamic block with no `save()`, so Quill sends `<!-- wp:footnotes /-->` plus a `meta.footnotes` JSON array. `extractFootnotes`/`inlineFootnotes` in `editor-transforms.js` are the split and its inverse; `window.setContent(html, footnotesJSON)` and `window.getFootnotes()` are the bridge. Full contract, migration behaviour and the marker-anchor rule: `docs/footnotes-meta.md`.
- `Sources/QuillKit/Views/Media/GallerySheet.swift` — native picker for creating a WordPress gallery (multi-select media grid, reorderable selection, columns/crop/link-to/size controls); posts `.insertGalleryData` which `EditorCoordinator` turns into a `window.insertGallery(json)` call
- **Adding a block setting is one line in `block-settings.js`** — the registry names how each Gutenberg setting draws (`carried`, `flagClass`, `valueClass`, `style`, `attr`), and the machinery derives the Tiptap attribute (`withBlockSettings` in `editor.html`), the delimiter key (`attrsFromSettings`/`ownedAttrsFor` in `block-descriptors.js`) and the toolbar control (`_buildSettingControl`) from it. A `control` field adds the toolbar control; `propagate: '<node>'` also writes the value to every descendant of that type in the same transaction, which is what core's twice-stored accordion icon settings need. Never write a `sourced` setting to the delimiter — WordPress reads those back out of the markup. Two settings the registry cannot express are document structure, not settings, and stay hand-written: a table's `<thead>`/`<tfoot>` sections (`tableRow.rowType`) and its caption's attachment to the figure.
- **An attribute no block setting names is carried anyway** — every node wrapped by `withBlockAttrs` snapshots its element's attributes on load (`withRawAttrs` in `editor.html`) and replays them on save in the source's own order, with the node's own values winning. That is what keeps columns' `is-not-stacked-on-mobile`, a details `name` and an ordered list's `reversed` after an edit, none of which has a registry entry. Two lists bound it: `RAW_ATTRS_MODELED` names the attributes a node draws itself, which must never be replayed or a value the user just turned off comes back — a node whose class any setting writes lists `class` there; `RAW_ATTRS_EXEMPT` holds the four figure blocks, which rebuild their figure from modelled attributes instead. A setting that draws a *child element* still needs a registry entry.
- **Unsupported block preservation** — on load, `window.setContent` (and `_exitCodeView`'s user-edited branch) run `post_content` through `wrapUnsupportedBlocks`, which uses WordPress's own parser to enumerate top-level blocks and replaces each one Quill cannot hold with a `div.wp-block-quill-unsupported` carrying its exact source; `toWordPressHTML` swaps each wrapper back for that source verbatim on save. Covers Custom HTML, shortcodes, the self-closing dynamic blocks (Calendar, Navigation, synced patterns), page breaks, Read More, and any third-party block that skips the `wp-block-` class convention. `_reportBlocksAtRisk` separately compares the parser's block list against the parsed document and posts `blocksAtRisk` to Swift, which shows `BlockRiskAlarm`'s banner and refuses to save (and suspends autosave) until acknowledged. Spec: `docs/superpowers/specs/2026-09-12-unsupported-block-preservation-design.md`.
- `gutenbergPassthrough` Tiptap node (`editor.html`) — catches any `wp-block-*` classed, non-`<figure>` element no other parse rule claims (Accordion, Columns, Group, third-party blocks), preserves it byte-for-byte as a static "Not editable in the visual editor" card. See `docs/superpowers/specs/2026-07-12-gutenberg-passthrough-block-design.md` and the `toWordPressHTML` gotcha above about its shielding window.

## Maintaining Gutenberg HTML compatibility

All WordPress/Gutenberg HTML compatibility lives in three files:

1. **`toWordPressHTML(html)`** in `editor-transforms.js` (line 16) — called on every save/content-change. Transforms Tiptap's internal HTML into Gutenberg-format HTML before sending to Swift. `renderHTML` on `ResizableImage` now always outputs `<figure><img ...><figcaption/></figure>`; `toWordPressHTML` annotates existing figures (adds classes, moves alignment, handles caption) rather than wrapping bare `<img>` tags. Edit this when WordPress changes expected output format.

2. **`ResizableImage.parseHTML()`** (~line 1079 in `editor.html`) — custom parse rule for `<figure class="wp-block-image">` that extracts image attrs (including alignment) from Gutenberg figure wrappers on load.

3. **`block-descriptors.js`** — maps each Tiptap node name to its Gutenberg block name, shape, `attrsFrom`, and `ownedAttrs`. `wrapInDelimiters` in `editor-transforms.js` emits `<!-- wp:name -->` delimiters for every descriptor, so teaching Quill a new modeled block is a descriptor entry, not another `toWordPressHTML` pass.

See `Sources/QuillKit/Resources/CLAUDE.md` for the current per-element output reference table, and the `update-gutenberg-html-format` skill for the update workflow.

## Code view

`editor.html` has a `</>` toggle button (`#btn-code-view`) in the toolbar's utility group (alongside spell-check and image-align controls). Clicking it switches the Tiptap editor for a `<textarea id="code-editor">` showing the raw WordPress HTML.

**State:** `codeViewActive` (boolean, JS module-level) tracks which mode is active.

**`_enterCodeView()` / `_exitCodeView()`** — helpers that toggle DOM visibility, the button's `.active` class, and the disabled state of all other toolbar controls. `_exitCodeView()` compares the textarea value against `_codeViewOriginal`; if the user made no changes, `editor.commands.setContent` is skipped entirely (preserving Tiptap's visual state). Only when the user actually edited the HTML does `_exitCodeView()` call `setContent(html, false)` to parse it back into Tiptap.

**`window.getContent()`** returns `textarea.value` when `codeViewActive`, otherwise `_rawHTML` if set (the last raw HTML loaded from WordPress), or `toWordPressHTML(editor.getHTML())` after visual edits — so Swift's save/autosave paths work correctly from either mode.

**`window.setContent()`** exits code view silently (without round-tripping the textarea through Tiptap) before loading the new HTML, so switching posts always lands in visual mode.

**`_rawHTML`** — stores the last raw HTML passed to `setContent()` or saved from code view. Cleared on every Tiptap `update` event so Tiptap becomes the source of truth after visual edits. Used by `getContent()` so saving without any visual edits sends the original WordPress HTML (including block comments) back verbatim.

**`_rawHTMLOnLoad`** — like `_rawHTML` but never cleared by visual edits. Set in `setContent()` and updated in `_exitCodeView()` only when the user edited the textarea. Used by `_enterCodeView()` to seed the textarea, so block comments (e.g. `<!-- wp:gallery -->`) remain visible in code view even after the user has typed in the visual editor. After exiting code view with an edit, `_rawHTMLOnLoad` is updated to the code-view-edited HTML so subsequent re-entries show the latest code.

**`_codeViewOriginal`** — snapshot of the textarea value captured in `_enterCodeView()` (after formatting). `_exitCodeView()` compares `textarea.value` against this to detect whether the user changed anything. Set to `null` after comparison. Prevents unnecessary `setContent` calls (and the content wipe / compounding-whitespace regression they cause) when the user enters and exits code view without editing.

**`_codeViewChanged()`** — debounced `input` listener attached to the textarea on `_enterCodeView` and removed on `_exitCodeView`. Fires `contentChanged` to Swift on a 500 ms debounce, keeping `htmlContent` in sync so ⌘S saves code-view edits without requiring an explicit exit. Uses the same `debounce` variable as the visual editor; `_exitCodeView()` cancels any pending debounce and fires `contentChanged` directly with the final value.

**`formatHTML(html, doc)`** — pure DOM HTML pretty-printer in `editor-transforms.js`, called by `_enterCodeView`. Block elements indented, inline elements inline, void elements self-close, `<pre>` verbatim, HTML comment nodes (nodeType 8) preserved verbatim. 17 JS tests.

**Link extension:** `Link.configure({ openOnClick: false, HTMLAttributes: { target: null, rel: null } })` — the `target: null` and `rel: null` override the Tiptap Link default of `target="_blank" rel="noopener noreferrer nofollow"`, which would otherwise be added to every link.

## Known gotchas

Most file-specific gotchas moved to per-directory `CLAUDE.md` files on 2026-07-11 so they load only when Claude is working in that directory: `Sources/QuillKit/Resources/`, `Sources/QuillKit/Views/Editor/`, `Sources/QuillKit/API/`, `Sources/QuillKit/Views/Media/`, `Sources/QuillKit/Views/Sidebar/`, `Sources/QuillKit/App/`, `Sources/QuillKit/AI/`, `Sources/QuillKit/Views/AI/`, `Sources/QuillKit/Views/Settings/`, `Sources/QuillKit/Views/`. What's left here is cross-cutting or doesn't map to one directory.

- **`build.sh` copies each `Resources/` file by name — a new resource needs its own `cp` line or it 404s at runtime** — the assemble step lists `editor.html`, `AppIcon.icns`, `tiptap-bundle.js`, `editor-transforms.js`, `marked-bundle.js`, `block-parser-bundle.js`, `block-serializer.js`, `block-descriptors.js`, `block-settings.js` individually rather than copying the directory. Swift builds fine and tests pass (jsdom loads from the source tree), so a missing line only shows up as a silently undefined global in the running app.
- **A top-level `const` in a Resources JS file is NOT reachable as `window.x` from `editor.html`; only `function` declarations are** — classic scripts share one global lexical scope, so a top-level `const` is reachable by *bare identifier* from another classic script but never as a property of `window`/`globalThis`. This matters because `editor-transforms.js` resolves `block-descriptors.js` as `globalThis` in the browser and as a CommonJS `require` under Node, so `blockDescriptorRegistry.SOME_CONST` silently reads `undefined` in the app while working perfectly in every Node test. Cost a real bug on 2026-09-12: `blockNeedsWrapping` read `BLOCK_DESCRIPTORS` (a `const`) to decide which blocks Quill models, got an empty set in the browser, and wrapped every paragraph in the post into a frozen passthrough card — with the whole JS unit suite green. Anything a Resources file must expose across this boundary has to be a `function` declaration (`modelsBlockName` in `block-descriptors.js` is the pattern), or explicitly assigned to `window`. The only test that catches this class of bug is one running in the real-`editor.html`-in-jsdom harness, not the pure-Node one.
- **New JS dependencies get their own bundle script, not a `bundle-tiptap.sh` edit** — that script's package.json pins `"^2"`, so re-running it npm-installs Tiptap fresh and can drift the editor onto a newer 2.x. `Scripts/bundle-marked.sh` (→ `marked-bundle.js`, `window.MarkedBundle`) and `Scripts/bundle-block-parser.sh` (→ `block-parser-bundle.js`, `window.BlockParser`) are the pattern: same esbuild IIFE recipe, separate file, so adding a parser never forces an editor upgrade.
- **Synchronous image work must not run on the main actor** — `ImageConversion.prepareForUpload` is a blocking ImageIO decode + JPEG re-encode. Its two SwiftUI callers (`PostEditorView.handleDroppedImages`, `MediaSidebarSection.uploadFromDisk`) are MainActor-isolated via `View` conformance, so calling it inline froze the UI for the whole conversion — visibly, on a large photo. Both now wrap it in `await Task.detached(priority: .userInitiated) { … }.value`. `uploadPickedImage` was already off the main thread. Any new upload path must do the same.
- **Anything that writes `uploadStatus` must go through `dropTask`** — the `UploadStatusPill` (`DesignSystem.swift`, `View.uploadStatus(_:)`) and the toast share **one** bottom-center slot. Overlapping Finder drops raced on the shared `uploadStatus` state: a second batch dropped while the first was still uploading cleared the pill early and let a toast and the pill render on top of each other. `PostEditorView` holds `@State private var dropTask: Task<Void, Never>?` and `onImageFilesDropped` chains each batch onto the previous one (`await previous?.value`), so batches never interleave. Any future code path that shows the pill must join the same queue rather than writing `uploadStatus` directly. Pure SwiftUI view plumbing with no extractable helper — no unit test; manual check is in `docs/testing-plan.md` §7.4.
- **Ad-hoc signing** — `build.sh` signs with `-`; "Always Allow" on keychain prompts won't persist across rebuilds (irrelevant now that credentials use file storage, but WKWebView may still prompt once per binary for its own internal keychain use)
- **Editor link colour is one CSS variable per theme** — `--link` on `:root` (light) and `body.dark` (dark) in `editor.html`. Prose links, footnote markers and footnote backrefs all read `var(--link)`; none carries its own colour. Driven by `body.dark` alone, not `prefers-color-scheme` — the media-query fallback the toolbar chrome uses would paint dark-theme links when the app is explicitly light on a dark system. Markers and backrefs used the amber accent (`#b45309`/`#d97706`) until 2026-09-13, and light prose links had no rule at all, falling through to WebKit's default `#0000EE`.
- **Color tokens & surface components** — `Color.wpSidebarBg` (#F2F1EF light) and `Color.wpPanelBg` (#F4F3F1 light) are in `DesignSystem.swift` with `NSColor` dynamic providers. `NSColor.wpSidebarBg` is also defined as a direct `NSColor` extension (used for `window.backgroundColor`). Do not use `NSColor.windowBackgroundColor` in sidebars/panels. Surface components: `WarmSidebarBackground` (sidebar bg with shimmer gradient), `WarmPanelHeaderBackground` (editor header + settings panel bg), `SoftPanelBoundary` (vertical boundary between panes — replaces `Divider()`), `SoftHorizontalDivider` (horizontal separator — replaces `Divider()`), `PanelInteriorFade(from:)` (edge fade for scroll lists). Use these instead of plain `Divider()`.
- **`.textFieldStyle(.plain)` is the house style for every text field** — with `.padding(5–7)` and `.overlay(RoundedRectangle(cornerRadius: 5).stroke(.separator, lineWidth: 1))`. Every field in the app follows it (`PostSettingsPanel`'s slug field is the reference). `.roundedBorder` draws a system blue focus ring that appears nowhere else, so it reads as a bug the moment a field is clicked.
- **`Picker` needs `.rebuildsOnAppearanceChange()` or it keeps its old light/dark styling after a mode switch** — SwiftUI backs `Picker` with a real `NSPopUpButton` (`SwiftUIPopupButton`) and stamps an *explicit* `NSAppearance` on it when it configures the control. That stamp is never refreshed: on a light/dark switch the button's own host (`AppKitPlatformViewHost`) moves to the new appearance while the button keeps the old one, so it goes on drawing the previous bezel and label color — a light pill with dark text inside a dark panel, or near-invisible pale text on the light panel. Verified 2026-07-25 by dumping the live ancestor chain (`SwiftUIPopupButton explicit=DarkAqua` under `AppKitPlatformViewHost explicit=Aqua` under an all-`Aqua` window), and it reproduces with the title-bar fix fully disabled, so it is unrelated to `window.backgroundColor`. Nothing in a plain `Picker` subtree reads `colorScheme`, so SwiftUI never re-evaluates it and the stale stamp survives indefinitely. The `.rebuildsOnAppearanceChange()` modifier in `DesignSystem.swift` re-`.id()`s the subtree on `colorScheme`, forcing a fresh control with the current appearance — apply it to every new `Picker`. `DatePicker`, `Toggle`, and `Stepper` were checked in both directions and do **not** need it.
- **Verify saved draft HTML straight from SQLite rather than through the UI** — a local draft is written only on an explicit Save Draft, so `sqlite3 ~/Library/Application\ Support/Quill/drafts.db "select content from local_drafts order by updated_at desc limit 1;"` is the fastest ground truth for what Quill actually saves. The `autosaves` table holds remote posts only and stays stale for local drafts. Code view is not a substitute — it shows `formatHTML`-pretty-printed content, not the bytes.
- **jsdom and Chrome both lie about ProseMirror's empty-node caret** — an empty inline node view renders a `<br class="ProseMirror-trailingBreak">`, and in WebKit that break is the *only* caret target the node has. Hiding it (`display: none`) lays out correctly in Chrome and passes every jsdom test, while making the node completely uneditable in the real app. `:has()` on that break is no better: WebKit does not re-evaluate it when the break is removed. Track empty state with a class the node view sets in its constructor and `update()` instead — `FootnoteItemNodeView`'s `fn-item-empty` is the pattern. Any CSS that keys off ProseMirror's own rendering artifacts has to be checked in Quill, not a browser.
- **Background computer-use clicks do not reach the WKWebView's DOM handlers** — `app_click` reports ok but toolbar dropdowns never open and the editor does not respond. Testing editor UI needs `request_full_control`; the app_* tools are fine for reading the window.
- **Unicode curly quotes in Swift strings** — Swift treats `"` (U+201C) and `"` (U+201D) as string delimiters, identical to ASCII `"`. Do not use curly quotes inside string literals with interpolation — use escaped ASCII quotes `\"...\(value)...\"` instead.

## Docs

- Spec: `docs/superpowers/specs/2026-05-21-wp-mac-app-design.md`
- Plan: `docs/superpowers/plans/2026-05-21-wp-writer-implementation.md`
- Spec (delete): `docs/superpowers/specs/2026-05-22-delete-post-draft-design.md`
- Plan (delete): `docs/superpowers/plans/2026-05-22-delete-post-draft.md`
- Spec (media sidebar): `docs/superpowers/specs/2026-05-23-media-sidebar-design.md`
- Plan (media sidebar): `docs/superpowers/plans/2026-05-23-media-sidebar.md`
- Spec (link picker): `docs/superpowers/specs/2026-05-23-link-picker-design.md`
- Plan (link picker): `docs/superpowers/plans/2026-05-23-link-picker.md`
- Spec (image resize): `docs/superpowers/specs/2026-05-23-image-resize-design.md`
- Plan (image resize): `docs/superpowers/plans/2026-05-23-image-resize.md`
- Spec (AI writing): `docs/superpowers/specs/2026-05-23-ai-writing-design.md`
- Plan (AI writing): `docs/superpowers/plans/2026-05-23-ai-writing.md`
- Spec (AI style guide caching): `docs/superpowers/specs/2026-05-24-ai-style-guide-caching-design.md`
- Plan (AI style guide caching): `docs/superpowers/plans/2026-05-24-ai-style-guide-caching.md`
- Spec (gallery support): `docs/superpowers/specs/2026-07-08-gallery-support-design.md`
- Plan (gallery support): `docs/superpowers/plans/2026-07-08-gallery-support.md`
- Spec (image link-to-full-size): `docs/superpowers/specs/2026-07-10-image-link-to-full-design.md`
- Plan (image link-to-full-size): `docs/superpowers/plans/2026-07-10-image-link-to-full.md`
- Spec (Gutenberg passthrough block): `docs/superpowers/specs/2026-07-12-gutenberg-passthrough-block-design.md`
- Plan (Gutenberg passthrough block): `docs/superpowers/plans/2026-07-12-gutenberg-passthrough-block.md`
- Footnotes in post meta: `docs/footnotes-meta.md`
- End-user guide: `docs/user-guide.md`
- WordPress release markup audit routine (run once per WP major release; includes the WP 7.1 run record): `docs/wordpress-release-audit.md`
